app.baseUrl=http://localhost:8080

# Navegador: chrome | edge | firefox
browser=chrome

# true en CI / false en local para ver el navegador
headless=false

# Timeout (segundos) de las esperas explicitas de Selenium
timeout.seconds=10